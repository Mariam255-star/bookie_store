import 'package:bookie_store/core/services/dio/dio_provider.dart';

class AuthRepo {
  register(
    String email,
    String password,
    String name,
    String confirmPassword,
  ) async {
    // var res = await DioProvider.dio
  }
}
